# Retail Orders Database – Querying Fundamentals

## Overview
Objective: practice fundamental SQL operations on a retail dataset (Superstore) to extract business insights using `SELECT`, `WHERE`, `ORDER BY`, `GROUP BY`, `JOIN`.

## Database & Table
- **Database:** `retail_db`  
- **Table:** `superstoredataset`

## SQL Concepts Used
- `SELECT` – retrieve data  
- `WHERE` – filter rows  
- `ORDER BY` – sort results  
- `GROUP BY` + `SUM()` / `COUNT()` – aggregation  
- `LIMIT` – restrict number of rows  
- `MONTH()` – extract month from dates  
- Self `JOIN` – find customers with multiple orders

## Tools Used

| Tool                      | Purpose                          |
| ------------------------- | -------------------------------- |
| MySQL Server              | Database engine                  |
| MySQL Workbench           | SQL Query execution & CSV import |
| Kaggle Superstore Dataset | Source data                      |

------

## Repository Contents

| File                    | Description                         |
| ----------------------- | ----------------------------------- |
| `Task3.sql`             | Contains all SQL queries used       |
| `SuperstoreDataset.csv` | Dataset used                        |
| `Screenshots`           | Contains screenshots of SQL outputs |
| `README.md`             | readme file                         |

